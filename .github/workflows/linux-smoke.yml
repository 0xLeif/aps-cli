name: Linux smoke

# GitHub-hosted Linux build + smoke for the 0.x agent-ready milestone.
# Complements the private-repo macOS self-hosted CI/Trust workflows.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: linux-smoke-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  linux-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Install Swift
        uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.2"  # newest available on setup-swift for Ubuntu; avoids the 6.1.3 Observation linker bug

      - name: Swift version
        run: swift --version

      - name: Test (debug)
        run: swift test

      - name: Smoke CLI
        env:
          APS_BIN: .build/debug/aps
        run: ./Scripts/smoke.sh
