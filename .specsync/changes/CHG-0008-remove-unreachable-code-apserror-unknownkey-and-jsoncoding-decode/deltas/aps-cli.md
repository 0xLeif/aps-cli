# Remove unreachable code in aps-cli

## REMOVED

### REQUIREMENT REQ-aps-cli-005
Removed `unknownKey` from `APSError` coverage.

### SPEC SECTION Public API
Removed `unknownKey` from the export inventory.