---
change: CHG-0063-prepare-aps-v1-1-0-atomically-build-and-test-a-portable-linux-bundle-align-hom
artifact: docs
---

# Docs

- Update README release status, installer details, plugin version, and site
  design-system attribution.
- Update release readiness with resolved distribution and version blockers
  while leaving the final tag and provenance publication as the remaining
  operator step.
- Document the reviewed version-preparation command and
  `fledge release minor --no-bump --dry-run` plus final tag flow.
- Document that tofu-ux is upstream, `0xLeif/0x` is the consumed contribution
  fork, and the site pins one reviewed commit until a registry release exists.
